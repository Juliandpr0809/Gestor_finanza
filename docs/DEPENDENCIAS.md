# Dependencias del Proyecto

Este archivo contiene el listado de todas las dependencias instaladas en el proyecto.

Consulta `requirements.txt` en la carpeta `backend/` para la información de dependencias oficiales.
